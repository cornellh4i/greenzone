import React, { useEffect, useRef } from "react";
import * as d3 from "d3";

// declare what type of data your graph will accept 
interface Props {
  info: { x: number; y: number }[];
}

// create LineGraph 
const LineGraph = ({ info }: Props) => { };

// export
export default LineGraph;